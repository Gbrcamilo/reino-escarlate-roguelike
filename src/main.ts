import './style.css';

type Vec={x:number;y:number};type EnemyKind='crawler'|'archer'|'stitcher';type RoomKind='combat'|'refuge';type GameState='play'|'relic'|'room-cleared'|'transition-out'|'transition-in'|'room-intro'|'victory'|'defeat';type Particle=Vec&{vx:number;vy:number;life:number;maxLife:number;color:string;size:number};type Projectile=Vec&{vx:number;vy:number;radius:number;life:number};type Obstacle={x:number;y:number;w:number;h:number};type Relic={name:string;description:string;apply:()=>void};type Room={id:string;kind:RoomKind;title:string;subtitle:string;obstacles:Obstacle[];waves:EnemyKind[][]};
const W=960,H=540,canvas=document.querySelector<HTMLCanvasElement>('#game');if(!canvas)throw new Error('Canvas não encontrado.');const ctx=canvas.getContext('2d');if(!ctx)throw new Error('Canvas 2D indisponível.');const keys=new Set<string>(),pressed=new Set<string>();window.addEventListener('keydown',e=>{const k=e.key.toLowerCase();if(!keys.has(k))pressed.add(k);keys.add(k);if([' ','arrowup','arrowdown','arrowleft','arrowright'].includes(k))e.preventDefault()});window.addEventListener('keyup',e=>keys.delete(e.key.toLowerCase()));const clamp=(v:number,min:number,max:number)=>Math.max(min,Math.min(max,v)),length=(v:Vec)=>Math.hypot(v.x,v.y),normalized=(v:Vec):Vec=>{const l=length(v)||1;return{x:v.x/l,y:v.y/l}},distance=(a:Vec,b:Vec)=>Math.hypot(a.x-b.x,a.y-b.y),inside=(p:Vec,r:Obstacle,pad=0)=>p.x>r.x-pad&&p.x<r.x+r.w+pad&&p.y>r.y-pad&&p.y<r.y+r.h+pad;
const rooms:Room[]=[{id:'clearing',kind:'combat',title:'CLAREIRA DOS SUSSURROS',subtitle:'Ecos inquietos se aproximam da Caixa.',obstacles:[{x:308,y:202,w:72,h:72},{x:580,y:266,w:72,h:72},{x:430,y:390,w:96,h:42}],waves:[['crawler','crawler','archer'],['crawler','archer','archer','stitcher'],['stitcher','stitcher','archer','crawler','crawler']]},{id:'refuge',kind:'refuge',title:'REFÚGIO DE CINZAS',subtitle:'A caixa silencia. Escolha o que preservar.',obstacles:[{x:382,y:218,w:196,h:92}],waves:[]},{id:'thorns',kind:'combat',title:'JARDIM DOS ESPINHOS MORTOS',subtitle:'A floresta fecha os dentes ao redor de Pandora.',obstacles:[{x:212,y:188,w:88,h:54},{x:660,y:188,w:88,h:54},{x:382,y:350,w:196,h:44}],waves:[['archer','crawler','crawler'],['stitcher','archer','crawler','archer'],['stitcher','stitcher','archer','crawler','crawler']]}];
class Player{pos:Vec={x:W/2,y:H-76};facing:Vec={x:0,y:-1};hp=100;maxHp=100;momentum=100;maxMomentum=100;essence=0;boxCharge=0;relics:string[]=[];gluttony=0;wrath=0;bleed=false;chainCapture=false;fastSeal=false;attackTimer=0;dashTimer=0;dashCooldown=0;invulnerable=0;capturing=0;attackHit=false;powerCooldown=0;hungerTimer=0;update(dt:number){const input={x:(keys.has('d')?1:0)-(keys.has('a')?1:0),y:(keys.has('s')?1:0)-(keys.has('w')?1:0)},moving=input.x!==0||input.y!==0;if(moving)this.facing=normalized(input);if(pressed.has(' ')&&this.dashCooldown<=0&&moving&&this.momentum>=18&&canControl()){this.dashTimer=.16;this.dashCooldown=.55;this.invulnerable=.2;this.momentum-=18}const speed=this.dashTimer>0?880:this.capturing>0?80:235;if(moving&&canControl()){const old={...this.pos},d=normalized(input);this.pos.x+=d.x*speed*dt;this.pos.y+=d.y*speed*dt;this.pos.x=clamp(this.pos.x,38,W-38);this.pos.y=clamp(this.pos.y,78,H-38);if(currentRoom.obstacles.some(o=>inside(this.pos,o,15)))this.pos=old}if(pressed.has('j')&&this.attackTimer<=0&&this.capturing<=0&&canControl()){this.attackTimer=.23;this.attackHit=false}this.attackTimer=Math.max(0,this.attackTimer-dt);this.dashTimer=Math.max(0,this.dashTimer-dt);this.dashCooldown=Math.max(0,this.dashCooldown-dt);this.invulnerable=Math.max(0,this.invulnerable-dt);this.capturing=Math.max(0,this.capturing-dt);this.powerCooldown=Math.max(0,this.powerCooldown-dt);this.momentum=clamp(this.momentum+18*dt,0,this.maxMomentum);this.boxCharge=clamp(this.boxCharge-2.4*dt,0,100);if(this.gluttony>0&&gameState==='play'){this.hungerTimer+=dt;if(this.hungerTimer>8){this.hp=Math.max(1,this.hp-dt*1.2)}}}hurt(a:number){if(this.invulnerable>0)return;this.hp=Math.max(0,this.hp-a);this.invulnerable=.5;this.capturing=0;if(this.wrath>0)this.boxCharge=clamp(this.boxCharge+8*this.wrath,0,100)}}
class Enemy{pos:Vec;readonly kind:EnemyKind;radius:number;hp:number;maxHp:number;speed:number;alive=true;hitFlash=0;attackTimer=0;dashTimer=0;shootTimer=.8;bleedTimer=0;constructor(kind:EnemyKind,x:number,y:number){this.kind=kind;this.pos={x,y};this.radius=kind==='stitcher'?28:kind==='archer'?18:21;this.maxHp=kind==='stitcher'?110:kind==='archer'?55:80;this.hp=this.maxHp;this.speed=kind==='stitcher'?75:kind==='archer'?68:105}get ruptured(){return this.alive&&this.hp<=this.maxHp*.25}get eco(){return this.kind==='stitcher'?'Fúria':this.kind==='crawler'?'Fome':'Vidro'}update(dt:number){if(!this.alive)return;const to={x:player.pos.x-this.pos.x,y:player.pos.y-this.pos.y},d=length(to),n=normalized(to);if(this.kind==='archer'){if(d<185){this.pos.x-=n.x*this.speed*dt;this.pos.y-=n.y*this.speed*dt}if(d>270){this.pos.x+=n.x*this.speed*dt;this.pos.y+=n.y*this.speed*dt}if(this.shootTimer<=0&&d<360){projectiles.push({x:this.pos.x,y:this.pos.y,vx:n.x*270,vy:n.y*270,radius:6,life:2.4});this.shootTimer=1.25}}else if(this.kind==='stitcher'){if(this.dashTimer>0){this.pos.x+=n.x*330*dt;this.pos.y+=n.y*330*dt}else if(d>80){this.pos.x+=n.x*this.speed*dt;this.pos.y+=n.y*this.speed*dt}if(d<210&&this.attackTimer<=0&&this.dashTimer<=0){this.dashTimer=.32;this.attackTimer=1.2}}else if(d>48){this.pos.x+=n.x*this.speed*dt;this.pos.y+=n.y*this.speed*dt}if(d<this.radius+25&&this.attackTimer<=0&&this.kind!=='archer'){player.hurt(this.kind==='stitcher'?17:11);this.attackTimer=.75}if(currentRoom.obstacles.some(o=>inside(this.pos,o,this.radius))){this.pos.x-=n.x*this.speed*dt*2;this.pos.y-=n.y*this.speed*dt*2}this.attackTimer=Math.max(0,this.attackTimer-dt);this.dashTimer=Math.max(0,this.dashTimer-dt);this.shootTimer=Math.max(0,this.shootTimer-dt);this.hitFlash=Math.max(0,this.hitFlash-dt);this.bleedTimer=Math.max(0,this.bleedTimer-dt);if(this.bleedTimer>0)this.hp-=4*dt;if(this.hp<=0)this.alive=false}hurt(a:number){this.hp-=a;this.hitFlash=.12;if(player.bleed||player.wrath>=2)this.bleedTimer=2.6;if(this.hp<=0){this.alive=false;return true}return false}}
let player:Player,currentRoom:Room,roomIndex=0,enemies:Enemy[]=[],projectiles:Projectile[]=[],particles:Particle[]=[],wave=1,gameState:GameState='room-intro',roomCleared=false,selectedRelics:Relic[]=[],transitionTimer=0,transitionAlpha=0,introTimer=1.5,message='',messageTimer=0,elapsed=0,captures=0,kills=0,refugeUsed=false;
function canControl(){return gameState==='play'||gameState==='room-cleared'}function spawn(){const p=[{x:110,y:130},{x:850,y:130},{x:110,y:430},{x:850,y:420},{x:480,y:110}];return p[Math.floor(Math.random()*p.length)]}function say(t:string,time=1.6){message=t;messageTimer=time}function enterRoom(i:number){roomIndex=i;currentRoom=rooms[i];enemies=[];projectiles=[];particles=[];selectedRelics=[];wave=1;roomCleared=false;refugeUsed=false;player.pos={x:W/2,y:H-76};player.facing={x:0,y:-1};if(currentRoom.kind==='combat')spawnWave(1);gameState='room-intro';introTimer=1.5}function spawnWave(n:number){wave=n;enemies=(currentRoom.waves[n-1]??[]).map(k=>{const p=spawn();return new Enemy(k,p.x,p.y)});projectiles=[];roomCleared=false;gameState='play';say(`Onda ${wave} de ${currentRoom.waves.length} — Portas seladas.`,1.8)}function reset(){player=new Player();roomIndex=0;captures=0;kills=0;elapsed=0;transitionAlpha=0;enterRoom(0)}function dust(pos:Vec,color:string,count:number){for(let i=0;i<count;i++){const a=Math.random()*Math.PI*2,s=45+Math.random()*135;particles.push({x:pos.x,y:pos.y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:.32+Math.random()*.38,maxLife:.7,color,size:2+Math.random()*4})}}
function attack(){if(player.attackTimer<=0||player.attackHit)return;const c={x:player.pos.x+player.facing.x*42,y:player.pos.y+player.facing.y*42};for(const e of enemies){if(!e.alive||distance(c,e.pos)>=e.radius+38)continue;player.attackHit=true;const killed=e.hurt(20);player.momentum=clamp(player.momentum+7,0,player.maxMomentum);player.hungerTimer=0;dust(e.pos,'#d7263d',9);if(killed){kills++;say('Eco dissipado. Sem Essência.',1.1)}break}}
function capture(){if(!pressed.has('f')||player.capturing>0)return;const t=enemies.find(e=>e.ruptured&&distance(e.pos,player.pos)<=78);if(!t){if(enemies.some(e=>e.ruptured))say('Aproxime-se do Eco em Ruptura.',1.2);return}player.capturing=player.fastSeal?.32:.58;t.alive=false;captures++;player.essence++;player.boxCharge=clamp(player.boxCharge+24,0,100);player.momentum=clamp(player.momentum+24,0,player.maxMomentum);player.hungerTimer=0;if(t.kind==='crawler'){player.gluttony=Math.min(3,player.gluttony+1);player.hp=clamp(player.hp+8+player.gluttony*2,0,player.maxHp)}if(t.kind==='stitcher')player.wrath=Math.min(3,player.wrath+1);say(`Eco de ${t.eco} selado · Lacre ${t.kind==='crawler'?'Gula':'Ira'} elevado`,1.8);dust(t.pos,t.kind==='stitcher'?'#d7263d':'#b8a84b',25);if(player.chainCapture)for(const e of enemies)if(e.alive&&distance(e.pos,t.pos)<115)e.hurt(16);if(player.gluttony>0&&player.wrath>0&&t.bleedTimer>0){player.hp=clamp(player.hp+12,0,player.maxHp);dust(t.pos,'#f0d8ac',18);say('RESSONÂNCIA: BANQUETE CARMESIM',2.2)}}
function boxPower(){if(!pressed.has('q')||player.powerCooldown>0||!canControl())return;if(player.wrath>player.gluttony&&player.wrath>0){player.powerCooldown=3.8;const radius=92+player.wrath*18;for(const e of enemies)if(e.alive&&distance(e.pos,player.pos)<radius){e.hurt(24+player.wrath*8);e.bleedTimer=3}player.boxCharge=clamp(player.boxCharge+12,0,100);dust(player.pos,'#d7263d',34);say('MARTELO DA IRA',1.2)}else if(player.gluttony>0){player.powerCooldown=4.2;for(const e of enemies)if(e.alive&&distance(e.pos,player.pos)<140){const n=normalized({x:player.pos.x-e.pos.x,y:player.pos.y-e.pos.y});e.pos.x+=n.x*55;e.pos.y+=n.y*55;e.hurt(10+player.gluttony*5);if(e.ruptured)player.hp=clamp(player.hp+5,0,player.maxHp)}dust(player.pos,'#b8a84b',30);say('MANDÍBULA DO VAZIO',1.2)}else say('A Caixa ainda não possui um Lacre dominante.',1.3)}
function relicPool(){const all:Relic[]=[{name:'Espinho de Rosa Morta',description:'Ataques aplicam sangramento.',apply:()=>{player.bleed=true}},{name:'Fio da Tecelã',description:'Capturas ferem inimigos próximos.',apply:()=>{player.chainCapture=true}},{name:'Botas da Cinderela Quebrada',description:'Capturas são mais rápidas.',apply:()=>{player.fastSeal=true}},{name:'Lanterna da Avó',description:'+35 de Vida máxima e cura 25.',apply:()=>{player.maxHp+=35;player.hp=clamp(player.hp+25,0,player.maxHp)}},{name:'Chave de Marfim',description:'+1 Essência e reduz Carga.',apply:()=>{player.essence++;player.boxCharge=clamp(player.boxCharge-30,0,100)}}];return all.sort(()=>Math.random()-.5).slice(0,3)}function choose(i:number){const r=selectedRelics[i];if(!r)return;r.apply();player.relics.push(r.name);selectedRelics=[];if(wave<currentRoom.waves.length)spawnWave(wave+1);else{roomCleared=true;gameState='room-cleared';say('A porta dourada está aberta. Atravesse para continuar.',99)}}function refuge(){if(refugeUsed||!pressed.has('f')||distance(player.pos,{x:W/2,y:H/2})>90)return;refugeUsed=true;player.hp=clamp(player.hp+32,0,player.maxHp);player.boxCharge=clamp(player.boxCharge-35,0,100);player.momentum=player.maxMomentum;roomCleared=true;gameState='room-cleared';say('Cinzas acolhidas: Vida, Ímpeto e Caixa estabilizados.',99);dust({x:W/2,y:H/2},'#d9a441',34)}function atExit(){return roomCleared&&player.pos.y<92&&player.pos.x>W/2-50&&player.pos.x<W/2+50}function startTrans(){gameState='transition-out';transitionTimer=.5;transitionAlpha=0}function next(){if(roomIndex>=rooms.length-1){gameState='victory';say('O Bosque se curva diante da Caixa.',99);return}enterRoom(roomIndex+1);gameState='transition-in';transitionTimer=.5;transitionAlpha=1}function trans(dt:number){if(gameState==='transition-out'){transitionTimer-=dt;transitionAlpha=clamp(1-transitionTimer/.5,0,1);if(transitionTimer<=0)next()}else if(gameState==='transition-in'){transitionTimer-=dt;transitionAlpha=clamp(transitionTimer/.5,0,1);if(transitionTimer<=0){gameState='room-intro';introTimer=1.5}}else if(gameState==='room-intro'){introTimer-=dt;if(introTimer<=0){gameState='play';if(currentRoom.kind==='refuge')say('Aproxime-se do altar e pressione F para acolher as cinzas.',99)}}}function moveShots(dt:number){projectiles=projectiles.filter(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.life-=dt;if(distance(p,player.pos)<p.radius+15){player.hurt(12);return false}return p.life>0&&p.x>0&&p.x<W&&p.y>58&&p.y<H})}
function update(dt:number){if(pressed.has('r')){reset();pressed.clear();return}elapsed+=dt;if(gameState==='play'||gameState==='room-cleared'){player.update(dt);if(currentRoom.kind==='combat'&&gameState==='play'){for(const e of enemies)e.update(dt);attack();capture();boxPower();moveShots(dt);enemies=enemies.filter(e=>e.alive);if(enemies.length===0&&!roomCleared){gameState='relic';selectedRelics=relicPool();say('Onda vencida. Escolha uma relíquia.',99)}if(player.hp<=0)gameState='defeat'}if(currentRoom.kind==='refuge')refuge();if(gameState==='room-cleared'&&atExit())startTrans()}if(gameState==='relic'){if(pressed.has('1'))choose(0);if(pressed.has('2'))choose(1);if(pressed.has('3'))choose(2)}if(gameState==='transition-out'||gameState==='transition-in'||gameState==='room-intro')trans(dt);messageTimer=Math.max(0,messageTimer-dt);particles=particles.filter(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=.92;p.vy*=.92;p.life-=dt;return p.life>0});pressed.clear()}
function bar(x:number,y:number,w:number,v:number,max:number,c:string,label:string){ctx.fillStyle='#0a0b11';ctx.fillRect(x,y,w,14);ctx.fillStyle='#352f3d';ctx.fillRect(x+2,y+2,w-4,10);ctx.fillStyle=c;ctx.fillRect(x+2,y+2,(w-4)*(v/max),10);ctx.fillStyle='#d8d2c5';ctx.font='600 13px Satoshi, sans-serif';ctx.fillText(label,x,y-7)}function arena(){ctx.fillStyle='#10121b';ctx.fillRect(0,0,W,H);ctx.strokeStyle='rgba(216,210,197,.055)';ctx.lineWidth=1;for(let x=0;x<=W;x+=48){ctx.beginPath();ctx.moveTo(x,58);ctx.lineTo(x,H);ctx.stroke()}for(let y=58;y<=H;y+=48){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(W,y);ctx.stroke()}ctx.fillStyle='#171725';ctx.fillRect(0,0,W,58);ctx.fillStyle='#d8d2c5';ctx.font='700 21px Boska, Georgia, serif';ctx.fillText(currentRoom.title,28,37);ctx.fillStyle='#6d6875';ctx.font='500 13px Satoshi, sans-serif';ctx.fillText(`Sala ${roomIndex+1}/${rooms.length} · Pandora e a Caixa dos Ecos`,665,34);for(const o of currentRoom.obstacles){ctx.fillStyle='#2a2834';ctx.fillRect(o.x,o.y,o.w,o.h);ctx.strokeStyle='#6d6875';ctx.lineWidth=2;ctx.strokeRect(o.x,o.y,o.w,o.h);ctx.fillStyle='rgba(216,210,197,.06)';ctx.fillRect(o.x+7,o.y+7,o.w-14,7)}if(currentRoom.kind==='refuge'){ctx.save();ctx.strokeStyle='#d9a441';ctx.lineWidth=3;ctx.shadowColor='#d9a441';ctx.shadowBlur=16;ctx.beginPath();ctx.arc(W/2,H/2,54+Math.sin(elapsed*4)*3,0,Math.PI*2);ctx.stroke();ctx.restore();ctx.fillStyle='#3b2a18';ctx.fillRect(W/2-18,H/2-18,36,36);ctx.strokeStyle='#d9a441';ctx.strokeRect(W/2-18,H/2-18,36,36)}const door=roomCleared?'#d9a441':'#6b1e2f';ctx.fillStyle=door;ctx.fillRect(W/2-44,58,88,10);ctx.fillRect(W/2-44,H-10,88,10);ctx.fillStyle='#6d6875';ctx.font='600 13px Satoshi, sans-serif';ctx.fillText(roomCleared?'PORTA DOURADA ABERTA · SIGA PARA O NORTE':currentRoom.kind==='combat'?`PORTAS SELADAS · ONDA ${wave}/${currentRoom.waves.length}`:'ALTAR DE CINZAS · APROXIME-SE',320,H-20)}
function box(){const x=player.pos.x-26,y=player.pos.y-16+Math.sin(elapsed*4)*3;ctx.save();if(player.capturing>0){ctx.shadowColor='#d9a441';ctx.shadowBlur=20}ctx.fillStyle='#3b2a18';ctx.fillRect(x-8,y-8,16,16);ctx.strokeStyle='#d9a441';ctx.lineWidth=2;ctx.strokeRect(x-8,y-8,16,16);ctx.fillStyle='#d9a441';ctx.fillRect(x-2,y-8,4,16);ctx.restore()}function hero(){ctx.save();ctx.translate(player.pos.x,player.pos.y);if(player.invulnerable>0&&Math.floor(player.invulnerable*20)%2===0)ctx.globalAlpha=.45;ctx.fillStyle='#6b1e2f';ctx.beginPath();ctx.moveTo(-15,17);ctx.lineTo(0,-24);ctx.lineTo(16,17);ctx.closePath();ctx.fill();ctx.fillStyle='#d8d2c5';ctx.beginPath();ctx.arc(0,-8,10,0,Math.PI*2);ctx.fill();ctx.fillStyle='#d9a441';ctx.fillRect(-5,1,10,8);ctx.strokeStyle='#d8d2c5';ctx.lineWidth=5;ctx.lineCap='round';ctx.beginPath();ctx.moveTo(player.facing.x*7,player.facing.y*7);ctx.lineTo(player.facing.x*28,player.facing.y*28);ctx.stroke();if(player.attackTimer>0){ctx.strokeStyle='#f0d8ac';ctx.lineWidth=4;ctx.beginPath();ctx.arc(0,0,48,Math.atan2(player.facing.y,player.facing.x)-.7,Math.atan2(player.facing.y,player.facing.x)+.7);ctx.stroke()}ctx.restore();box()}function foe(e:Enemy){if(e.ruptured){ctx.save();ctx.strokeStyle='#d9a441';ctx.lineWidth=3;ctx.shadowColor='#d9a441';ctx.shadowBlur=12;ctx.beginPath();ctx.arc(e.pos.x,e.pos.y,e.radius+10+Math.sin(elapsed*9)*2,0,Math.PI*2);ctx.stroke();ctx.restore()}ctx.save();ctx.translate(e.pos.x,e.pos.y);ctx.fillStyle=e.hitFlash>0?'#f3c7ce':e.kind==='archer'?'#645284':e.kind==='stitcher'?'#7a342e':'#6b1e2f';if(e.kind==='archer'){ctx.beginPath();ctx.moveTo(0,-e.radius);ctx.lineTo(e.radius,e.radius);ctx.lineTo(-e.radius,e.radius);ctx.closePath();ctx.fill()}else if(e.kind==='stitcher')ctx.fillRect(-e.radius,-e.radius+5,e.radius*2,e.radius*2-5);else{ctx.beginPath();ctx.arc(0,0,e.radius,0,Math.PI*2);ctx.fill()}ctx.fillStyle='#24131c';ctx.fillRect(-10,-5,20,13);ctx.fillStyle='#d8d2c5';ctx.fillRect(-7,-1,4,4);ctx.fillRect(4,-1,4,4);if(e.ruptured){ctx.fillStyle='#d9a441';ctx.fillRect(-2,7,4,8)}ctx.restore();bar(e.pos.x-31,e.pos.y-e.radius-22,62,e.hp,e.maxHp,e.ruptured?'#d9a441':'#6b1e2f','')}
function hud(){bar(26,86,220,player.hp,player.maxHp,'#d7263d','VIDA');bar(26,122,220,player.momentum,player.maxMomentum,'#8c6ccf','ÍMPETO');bar(26,158,220,player.boxCharge,100,player.boxCharge>=75?'#d7263d':player.boxCharge>=50?'#d98441':'#d9a441','CARGA DA CAIXA');ctx.fillStyle='#d8d2c5';ctx.font='600 14px Satoshi, sans-serif';ctx.fillText(`Essência: ${player.essence}`,738,86);ctx.fillText(`Ecos selados: ${captures}`,738,110);ctx.fillText(`Vultos dissipados: ${kills}`,738,134);ctx.fillStyle='#d9a441';ctx.font='700 12px Satoshi, sans-serif';ctx.fillText('LACRES DA CAIXA',26,210);const seals=[{n:'GULA',v:player.gluttony,c:'#b8a84b'},{n:'IRA',v:player.wrath,c:'#d7263d'}];seals.forEach((s,i)=>{const x=26+i*112;ctx.strokeStyle=s.c;ctx.lineWidth=2;ctx.strokeRect(x,220,96,30);ctx.fillStyle=s.v? s.c:'#302d36';ctx.fillRect(x+3,223,90*(s.v/3),24);ctx.fillStyle='#d8d2c5';ctx.font='700 11px Satoshi, sans-serif';ctx.fillText(`${s.n} ${s.v?'I'.repeat(s.v):'—'}`,x+7,241)});ctx.fillStyle=player.gluttony>0&&player.wrath>0?'#f0d8ac':'#6d6875';ctx.font='600 12px Satoshi, sans-serif';ctx.fillText(player.gluttony>0&&player.wrath>0?'RESSONÂNCIA DISPONÍVEL: BANQUETE CARMESIM':'Q · Poder do Lacre dominante',26,270);ctx.fillStyle='#d8d2c5';ctx.fillText(`Q: ${player.wrath>player.gluttony?'Martelo da Ira':player.gluttony>0?'Mandíbula do Vazio':'—'} ${player.powerCooldown>0?`(${player.powerCooldown.toFixed(1)}s)`:''}`,738,160);ctx.fillStyle='#d9a441';ctx.font='600 12px Satoshi, sans-serif';ctx.fillText(`Relíquias: ${player.relics.length?player.relics.join(' · '):'nenhuma'}`,26,292)}
function relicScreen(){ctx.fillStyle='rgba(10,11,17,.84)';ctx.fillRect(0,0,W,H);ctx.fillStyle='#d8d2c5';ctx.font='700 34px Boska, Georgia, serif';ctx.textAlign='center';ctx.fillText('A CAIXA OFERECE TRÊS CAMINHOS',W/2,116);ctx.fillStyle='#d9a441';ctx.font='500 16px Satoshi, sans-serif';ctx.fillText('Pressione 1, 2 ou 3 para escolher uma relíquia.',W/2,146);selectedRelics.forEach((r,i)=>{const x=112+i*252;ctx.fillStyle='#1c1b28';ctx.fillRect(x,190,220,190);ctx.strokeStyle='#d9a441';ctx.lineWidth=2;ctx.strokeRect(x,190,220,190);ctx.fillStyle='#d9a441';ctx.font='700 18px Satoshi, sans-serif';ctx.fillText(String(i+1),x+22,220);ctx.fillStyle='#d8d2c5';ctx.font='700 21px Boska, Georgia,serif';ctx.fillText(r.name,x+110,262);ctx.fillStyle='#b6b0a6';ctx.font='500 15px Satoshi, sans-serif';ctx.fillText(r.description,x+110,310)});ctx.textAlign='start'}function note(){if(messageTimer<=0||gameState==='relic')return;ctx.fillStyle='rgba(16,18,27,.84)';ctx.fillRect(205,178,550,72);ctx.strokeStyle='rgba(216,210,197,.25)';ctx.strokeRect(205,178,550,72);ctx.fillStyle='#d8d2c5';ctx.font='600 18px Satoshi, sans-serif';ctx.textAlign='center';ctx.fillText(message,W/2,222);ctx.textAlign='start'}function fade(){if(transitionAlpha<=0)return;ctx.save();ctx.fillStyle=`rgba(10,11,17,${transitionAlpha})`;ctx.fillRect(0,0,W,H);if(transitionAlpha>.68&&roomIndex<rooms.length){ctx.globalAlpha=(transitionAlpha-.68)/.32;ctx.fillStyle='#d8d2c5';ctx.font='700 32px Boska, Georgia, serif';ctx.textAlign='center';ctx.fillText(currentRoom.title,W/2,H/2-10);ctx.fillStyle='#d9a441';ctx.font='500 16px Satoshi, sans-serif';ctx.fillText(currentRoom.subtitle,W/2,H/2+24);ctx.textAlign='start'}ctx.restore()}function end(){if(gameState!=='victory'&&gameState!=='defeat')return;ctx.fillStyle='rgba(10,11,17,.82)';ctx.fillRect(0,0,W,H);ctx.fillStyle='#d8d2c5';ctx.font='700 42px Boska, Georgia, serif';ctx.textAlign='center';ctx.fillText(gameState==='victory'?'O BOSQUE SE CURVA DIANTE DA CAIXA':'A CAIXA SE FECHOU SOBRE PANDORA',W/2,218);ctx.fillStyle='#d9a441';ctx.font='600 18px Satoshi, sans-serif';ctx.fillText(`Ecos selados: ${captures} · Essência: ${player.essence}`,W/2,262);ctx.fillStyle='#d8d2c5';ctx.font='500 17px Satoshi, sans-serif';ctx.fillText('Pressione R para recomeçar a expedição.',W/2,306);ctx.textAlign='start'}function render(){arena();for(const e of enemies)foe(e);for(const p of projectiles){ctx.fillStyle='#8c6ccf';ctx.beginPath();ctx.arc(p.x,p.y,p.radius,0,Math.PI*2);ctx.fill()}hero();for(const p of particles){ctx.globalAlpha=p.life/p.maxLife;ctx.fillStyle=p.color;ctx.fillRect(p.x,p.y,p.size,p.size)}ctx.globalAlpha=1;hud();note();if(gameState==='relic')relicScreen();end();fade()}let last=performance.now();function loop(t:number){const dt=Math.min((t-last)/1000,.033);last=t;update(dt);render();requestAnimationFrame(loop)}reset();requestAnimationFrame(loop);